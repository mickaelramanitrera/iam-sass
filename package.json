{
  "name": "iam-sass",
  "version": "0.1.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "shadcn-add": "npx --workspace=web shadcn-ui@latest add",
    "workspace-add": "turbo gen workspace",
    "test": "turbo run test",
    "test-watch": "turbo run test:watch"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.10.6"
  }
}
